{
  "name": "abc",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=framework & npm run dev --workspace=playground",
    "build:fw": "npm run build --workspace=framework",
    "watch:fw": "npm run dev --workspace=framework",
    "test": "npm run test --workspace=framework",
    "format": "rome format --write framework",
    "lint": "rome check framework"
  },
  "devDependencies": {
    "rome": "^11.0.0"
  },
  "workspaces": [
    "framework",
    "playground"
  ]
}